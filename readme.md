# dotfiles

______________________________________________________________________

To use a background image in the terminal (or anywhere else), use
`$HOME/terminal.png`, then create a symlink to the actual image:

```
ln -s ./Pictures/andromeda.png ./terminal.png
```

______________________________________________________________________

The `./shell/workspace-indicators.css` is an optional stylesheet for a Gnome
extension called "Improved Workspace Indicators".

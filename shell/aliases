#### please please please
alias please='sudo $(history -p !!)'

#### git aliases
alias ga="git add -A"
alias gc="git commit -m"
alias gss="git status"
alias gps="git push origin main"
alias gpl="git pull origin main"
alias glo="git log --oneline"
alias co="git checkout"
alias com="git checkout main"
alias cob="git checkout -b"
alias wip='git add -A && git commit -m "wip" && git push'
alias gsq="git reset --soft main && git commit -m "

# Thanks to Jeffrey Way
nah () {
    git reset --hard
    git clean -df
    if [ -d ".git/rebase-apply" ] || [ -d ".git/rebase-merge" ]; then
        git rebase --abort
    fi
}

#### wp #### laravel #### php ####
alias installwp="wget http://wordpress.org/latest.tar.gz && tar xfz latest.tar.gz && mv wordpress/* ./ && rmdir ./wordpress/ && rm -f latest.tar.gz"
alias createwpuser="wp user create levon levon@drim.io --role=administrator --user_pass=levon"

alias a="php artisan"
alias t="php artisan tinker"
alias at="php artisan test"
alias atp="php artisan test --parallel"

alias pint="./vendor/bin/pint"
alias dump="composer dumpautoload"


#### npm aliases
alias npmw="npm run watch"
alias npmp="npm run production"
alias npmd="npm run dev"
alias npmb="npm run build"


#### careful!
## alias rm="echo 'This is not the command you are looking for 😋 Try trash-put, maybe'; false"
alias rm="trash-put"
alias jnjel="trash-put"

#### go on, do what you need to do
alias goon="sudo apt update && sudo apt upgrade -y"

#### one-time boot into the linux-surface kernel (check `uname -a` after upgrades,
#### version string here may drift and need updating)
alias boot-surface='sudo grub-reboot "Advanced options for Ubuntu>Ubuntu, with Linux 6.19.8-surface-3" && sudo reboot'


#### editors
alias vim=nvim
alias c="code ."
alias cx="code . && exit"
alias o="xdg-open "


#### navigation
alias ".."="cd .."
alias z="cd -"
alias ":q"="exit"
alias ":wq"="exit"


#### project-specific
alias "alias:create"="cd ~/code/projects/addy-io-cli/ && a alias:create"
alias "alias:list"="cd ~/code/projects/addy-io-cli/ && a alias:list"

alias untar='tar -xzvf'
alias findText='grep -rwn ./ -e'
alias countFiles='find . -type f | wc -l'
alias countFilesInDirs='for d in */; do echo "$(find "$d" -type f | wc -l) $d"; done | sort -nr'

#### other aliases
[ -f ~/.tmux/tmuxaliases ] && source ~/.tmux/tmuxaliases

